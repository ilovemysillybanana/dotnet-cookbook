# .NET Core Cookbook

This cookbook installs .NET Core for Windows and Linux(Also supports Mono).

### Supported Platforms
* Red Hat, Fedora, CentOS
* Debian, Ubuntu
* Windows

### Dependencies
* Chef 12.5+
* yum
* apt
* sudo
